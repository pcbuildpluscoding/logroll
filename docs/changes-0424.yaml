---
logroll:
  - source: __package__
    date: 6 Apr 2024
    notes: |
      import and convert streamlog/front/logfile.go with extra support files.
  - source: entry.go
    date: 6 Apr 2024
    notes: |
      remove Packet because it is not used for LogFile operations.
  - source: logroll.go, logfile.go and util.go
    date: 6 Apr 2024
    notes: |
      make a TraceLog type that wraps a bytes.Buffer and is usable to debug the internal LogFile workflow.
      add the new TraceLog as a traceLog property to LogFile
      it is applicable by adding a variadic traceLog bool argument to NewFile. 
      If true traceLog.running is set true. Add a DumpTrace method to LogFile for printing the traceLog to
      the output io.Writer.
  - source: logfile.go
    date: 7 Apr 2024
    notes: |
      Add a SetTrace method with a bool argument that sets the running status.
  - source: logroll.go
    date: 7 Apr 2024
    notes: |
      remove all the previous logroll version code.
  - source: entry.go
    date: 7 Apr 2024
    notes: |
      1. create a LogWriter interface that consumes a *LogEntry and which handles record formating or otherwise.
      2. change LogFile.Out and rOut properties from io.Writer to LogWriter
      3. change LogFile.addEntry to call Out(*LogEntry) instead of formatting a byte slice for an io.Writer to
         consume.
      4. comment out any LogFile methods that depend on LogFile.Out type = io.Writer
  - source: logfile.go
    date: 7 Apr 2024
    notes: |
      add traceLogging of all errors in LogFile.addEntry
      remove LogFile.remoteWriter property
  - source: util.go
    date: 7 Apr 2024
    notes: |
      change TraceLog.SetTraceMode to SetTrace, since the short form is self-explained by the bool argument.
  - source: logfile.go
    date: 7 Apr 2024
    notes: |
      change LogFile.Out to writer and rOut to rwriter
      add a new method called Writer() that returns io.Writer
  - source: entry.go
    date: 8 Apr 2024
    notes: |
      change LogEntry.Encode and Decode to use the time.UnixMicro call which records microseconds
  - source: __package__
    date: 8 Apr 2024
    notes: |
      add a .gitignore file to ignore temp and testing
  - source: logfile.go
    date: 8 Apr 2024
    notes: |
      1. add a Flush method to guarantee Subscriber receipt of the remainder logging content, before a completed job is
         disconnected from all services.
      2. remove the commented out old version of the getBytes method.
    review: not as useful as the Close method, which proved to be a good solution.
  - source: logfile.go
    date: 9 Apr 2024
    notes: |
      to LogFile add Fatal(error) and Closef(string,...interface{}), where Closef adds a record as normal and
      additionally adds an __EOF__ record.
  - source: logroll.go
    date: 10 Apr 2024
    notes: |
      1. remove the old design source code in logroll.go. remove formatter.go after moving Formatter to 
      2. create a local version of the logrus.Level type
      3. change provider function NewFile(level logrus.Level, logPath string) (*LogFile, error) 
         to WithFile(logPath string, ...Level) (*LogFile, error)
      4. change the default level applied in New() *LogFile constructor from DebugLevel to InfoLevel
  - source: text_formatter.go
    date: 10 Apr 2024
    notes: |
      create a rudimentary TextFormatter which has virtually no options except reportCaller bool, and prints 
      only the typical fields as effeciently as possible.
  - source: text_formatter.go
    date: 11 Apr 2024
    notes: |
      remove all logrus dependency from TextFormatter
      1. replace logrus.AllLevels with a local var allLevels with type []string and update TextFormatter.init to
         use it.
      2. create a getTrimCallerProps function that returns caller funcText and fileText, trimmed by default
      3. create MinimalFormatter that implements the rudimentary TextFormatter documented earlier.
  - source: formatter.go
    date: 11 Apr 2024
    notes: |
      1. move defaultTimestampFormat const declaration from formatter.go into logroll.go
      2. move Formatter type declaration from formatter.go into logroll.go and remove formatter.go
  - source: util.go
    date: 11 Apr 2024
    notes: |
      remove the redundant map value assignment in FlowRule.Value
  - source: entry.go
    date: 11 Apr 2024
    notes: |
      correct a minor bug in FileWriter struct, changing formatter type from *TextFormatter to Formatter
  - source: logroll.go
    date: 11 Apr 2024
    notes: |
      1. create a NewMinimalFormatter provider function
      2. in the New provider function, replace NewTextFormatter with NewMinimalFormatter
  - source: __package__
    date: 11 Apr 2024
    notes: |
      create a testing folder, and setup a testcase suit that proves MinimalFormatter is bug-free.
  - source: logroll.go
    date: logroll.go
    notes: |
      change Level.String to return a lowercase short form of the level value
  - source: entry.go
    date: 13 Apr 2024
    notes: |
      add a Close() error method to the LogWriter interface.
  - source: logfile.go
    date: 13 Apr 2024
    notes: |
      1. change LogFile.Closef(string, ...interface{}) to LogFile.Close()
      2. add a []io.Closer prop to FileWriter and append a closer if the io.Writer is also an io.Closer.
         the append closer action happens in FileWriter.SetWriter method
      3. change the code : 1. call lf.writer.Close() and 2. call lf.rWriter.Close() (if != nil)